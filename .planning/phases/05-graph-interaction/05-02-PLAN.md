---
phase: 05-graph-interaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/index.html
autonomous: true
requirements:
  - VIZN-04
  - INTR-01
  - INTR-05
  - INTR-06
  - INTR-07
  - INTR-08
must_haves:
  truths:
    - "Header bar contains search input, back/forward nav buttons, breadcrumb container, and quick-filter pills"
    - "Settings panel has an Analysis section with dead code and blast radius toggles"
    - "Settings panel Filters section has symbol type checkboxes, edge type checkboxes, and directory input"
    - "Display section has an expand mode dropdown"
    - "Command palette overlay markup exists with input and results container"
    - "Fit-to-screen button exists in bottom-left corner"
  artifacts:
    - path: "client/index.html"
      provides: "Complete HTML shell for all Phase 5 interaction features"
      contains: "command-palette"
  key_links:
    - from: "client/index.html"
      to: "client/graph.js"
      via: "DOM element IDs referenced by JS"
      pattern: "id=\"header-search\""
---

<objective>
Extend the HTML shell with all DOM elements needed for Phase 5 interaction features: restructured header bar, new panel sections, command palette overlay, and fit-to-screen button.

Purpose: Subsequent JS plans (03-05) wire behavior to these DOM elements. Creating the HTML first means JS plans can reference elements by ID without needing to create markup inline.

Output: Updated `client/index.html` with header bar (search, nav, breadcrumb, pills), Analysis panel section, extended Filters section, Display section expand dropdown, command palette overlay, and fit-to-screen button.
</objective>

<execution_context>
@.planning/phases/05-graph-interaction/05-UI-SPEC.md
@.planning/phases/05-graph-interaction/05-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- DOM element IDs that graph.js (Plans 03-05) will reference -->

Header elements:
- #btn-back, #btn-forward — navigation history buttons
- #header-search — always-visible search input
- #breadcrumb — breadcrumb trail container
- #quick-filters — container for pill buttons
- .pill[data-filter="functions|classes|imports|calls"] — quick-filter pill buttons

Panel elements:
- #toggle-dead-code — dead code overlay toggle
- #toggle-blast-radius — blast radius mode toggle
- #expand-mode — expand mode dropdown (orbital/force/stacked)
- #filter-dir — directory filter text input
- #filter-fn, #filter-class, #filter-type, #filter-hook, #filter-enum — symbol type checkboxes
- #filter-edge-import, #filter-edge-call, #filter-edge-typeref — edge type checkboxes

Overlay elements:
- #command-palette — centered overlay container
- #palette-input — command palette text input
- #palette-results — command palette result list
- #palette-backdrop — click-to-dismiss backdrop

Other:
- #btn-fit — fit-to-screen button (bottom-left)
- #focus-hint — "Press Esc to exit focus" label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure header bar with search, navigation, breadcrumb, and quick-filter pills</name>
  <files>client/index.html</files>
  <read_first>
    client/index.html
    .planning/phases/05-graph-interaction/05-UI-SPEC.md
  </read_first>
  <action>
Replace the current header bar (lines 143-146) with a three-part flex layout. The header keeps its existing 40px height, #1a1a1a background, and border-bottom style.

Replace:
```html
<div id="header">
  <span id="project-name"></span>
  <span id="stats"></span>
</div>
```

With:
```html
<div id="header">
  <div id="header-left" style="display:flex;align-items:center;gap:8px;">
    <button id="btn-back" title="Back" style="width:28px;height:28px;background:#333;border:1px solid #444;border-radius:6px;color:#999;cursor:pointer;display:flex;align-items:center;justify-content:center;opacity:0.4;cursor:default;" disabled>
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    </button>
    <button id="btn-forward" title="Forward" style="width:28px;height:28px;background:#333;border:1px solid #444;border-radius:6px;color:#999;cursor:pointer;display:flex;align-items:center;justify-content:center;opacity:0.4;cursor:default;" disabled>
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
    </button>
    <span id="project-name"></span>
    <input type="text" id="header-search" placeholder="Search symbols..." style="width:200px;padding:4px 8px;background:#333;border:1px solid #444;border-radius:6px;color:#ddd;font-size:13px;outline:none;" />
  </div>
  <div id="header-center" style="display:flex;align-items:center;gap:8px;">
    <div id="quick-filters" style="display:flex;gap:4px;">
      <button class="pill" data-filter="functions" style="padding:4px 10px;font-size:11px;border-radius:12px;border:1px solid #444;background:#333;color:#999;cursor:pointer;">Functions</button>
      <button class="pill" data-filter="classes" style="padding:4px 10px;font-size:11px;border-radius:12px;border:1px solid #444;background:#333;color:#999;cursor:pointer;">Classes</button>
      <button class="pill" data-filter="imports" style="padding:4px 10px;font-size:11px;border-radius:12px;border:1px solid #444;background:#333;color:#999;cursor:pointer;">Imports</button>
      <button class="pill" data-filter="calls" style="padding:4px 10px;font-size:11px;border-radius:12px;border:1px solid #444;background:#333;color:#999;cursor:pointer;">Calls</button>
    </div>
  </div>
  <div id="header-right" style="display:flex;align-items:center;gap:8px;">
    <span id="stats"></span>
  </div>
</div>
```

Add breadcrumb trail and focus hint just below the header (after the `<div id="header">...</div>` closing tag, before `<div id="graph">`):

```html
<div id="breadcrumb" style="position:absolute;top:48px;left:16px;z-index:100;display:flex;align-items:center;gap:4px;font-size:12px;color:#999;display:none;"></div>
<div id="focus-hint" style="position:absolute;top:48px;right:270px;z-index:100;font-size:11px;color:#666;display:none;">Press Esc to exit focus</div>
```

Add CSS for pill active state and header-search focus in the `<style>` block (before the closing `</style>` tag):

```css
.pill.active { background: #7f6df2 !important; color: #fff !important; border-color: #7f6df2 !important; }
.pill:hover { background: #444 !important; color: #ddd !important; }
.pill.active:hover { background: #6b5ce0 !important; }
#header-search:focus { border-color: #7f6df2; }
#btn-back:not(:disabled):hover, #btn-forward:not(:disabled):hover { background: #444; color: #ddd; }
#btn-back:not(:disabled), #btn-forward:not(:disabled) { opacity: 1; cursor: pointer; }
```
  </action>
  <verify>
    <automated>grep -c 'id="header-search"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="btn-back"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="breadcrumb"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'data-filter="functions"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="focus-hint"' /Users/vayu/Dev/Projects/cgraph/client/index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="header-search"` with placeholder "Search symbols..."
    - index.html contains `id="btn-back"` and `id="btn-forward"` buttons
    - index.html contains `id="breadcrumb"` container
    - index.html contains `id="quick-filters"` with 4 pill buttons (data-filter: functions, classes, imports, calls)
    - index.html contains `id="focus-hint"` with text "Press Esc to exit focus"
    - index.html contains `.pill.active` CSS rule with background #7f6df2
    - Header still has height 40px, #1a1a1a background
    - #project-name and #stats elements still exist
  </acceptance_criteria>
  <done>Header bar restructured with search input, nav buttons, breadcrumb, quick-filter pills, and focus hint. All IDs present for JS wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Add Analysis panel section, extend Filters section, add expand mode dropdown, command palette, and fit-to-screen button</name>
  <files>client/index.html</files>
  <read_first>
    client/index.html
    .planning/phases/05-graph-interaction/05-UI-SPEC.md
    .planning/phases/05-graph-interaction/05-PATTERNS.md
  </read_first>
  <action>
**1. Add Analysis section to the settings panel.**

Insert a new `.panel-section` AFTER the Filters section (after line ~177's closing `</div>`) and BEFORE the Display section (before line ~179). Per D-76, Analysis goes between Filters and Display. Copy the existing section pattern exactly:

```html
<div class="panel-section">
  <div class="section-header" data-section="analysis">
    <svg class="chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
    <span class="section-title">Analysis</span>
  </div>
  <div class="section-body" id="sec-analysis">
    <div class="ctrl-row">
      <span class="ctrl-label">Dead code</span>
      <label class="toggle-switch"><input type="checkbox" id="toggle-dead-code"><span class="toggle-track"></span></label>
    </div>
    <div class="ctrl-row" id="dead-code-stats" style="display:none;">
      <span class="ctrl-label" id="dead-code-count" style="font-size:10px;color:#666;"></span>
    </div>
    <div class="ctrl-row">
      <span class="ctrl-label">Blast radius</span>
      <label class="toggle-switch"><input type="checkbox" id="toggle-blast-radius"><span class="toggle-track"></span></label>
    </div>
    <div class="ctrl-row" id="blast-radius-prompt" style="display:none;">
      <span class="ctrl-label" style="font-size:10px;color:#666;">Click a node to see its blast radius</span>
    </div>
  </div>
</div>
```

**2. Extend the Filters section** with symbol type and edge type checkboxes.

Add these controls AFTER the existing orphan toggle row inside `#sec-filters` (after the `toggle-orphans` ctrl-row):

```html
<div style="margin-top:6px;margin-bottom:2px;"><span class="ctrl-label" style="font-weight:500;color:#dcddde;">Symbol types</span></div>
<div class="ctrl-row">
  <span class="ctrl-label">Function</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-fn" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Class</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-class" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Type</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-type" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Hook</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-hook" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Enum</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-enum" checked><span class="toggle-track"></span></label>
</div>
<div style="margin-top:6px;margin-bottom:2px;"><span class="ctrl-label" style="font-weight:500;color:#dcddde;">Edge types</span></div>
<div class="ctrl-row">
  <span class="ctrl-label">Import</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-edge-import" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Call</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-edge-call" checked><span class="toggle-track"></span></label>
</div>
<div class="ctrl-row">
  <span class="ctrl-label">Type reference</span>
  <label class="toggle-switch"><input type="checkbox" id="filter-edge-typeref" checked><span class="toggle-track"></span></label>
</div>
<div style="margin-top:6px;margin-bottom:2px;"><span class="ctrl-label" style="font-weight:500;color:#dcddde;">Directory</span></div>
<div class="search-wrap">
  <svg class="search-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
  <input type="text" class="search-input" id="filter-dir" placeholder="Filter by directory...">
</div>
```

**3. Add expand mode dropdown** to the Display section.

Add this ctrl-row AFTER the existing "Dir halos" row in `#sec-display` (after the halos-toggle ctrl-row):

```html
<div class="ctrl-row">
  <span class="ctrl-label">Expand mode</span>
  <select id="expand-mode" style="background:#333;border:1px solid #444;color:#ddd;font-size:11px;border-radius:4px;padding:2px 4px;outline:none;">
    <option value="orbital">Orbital</option>
    <option value="force">Force</option>
    <option value="stacked">Stacked</option>
  </select>
</div>
```

**4. Add command palette overlay.**

Add this AFTER the tooltip div (after `</div>` closing the tooltip, before `<button id="panel-toggle"`):

```html
<div id="palette-backdrop" style="display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(0,0,0,0.5);z-index:1000;"></div>
<div id="command-palette" style="display:none;position:fixed;top:48px;left:50%;transform:translateX(-50%);width:480px;background:#2a2a2a;border:1px solid #3a3a3a;border-radius:12px;z-index:1001;box-shadow:0 8px 32px rgba(0,0,0,0.6);">
  <input type="text" id="palette-input" placeholder="Jump to symbol or file..." style="width:100%;padding:12px 16px;background:transparent;border:none;border-bottom:1px solid #3a3a3a;color:#ddd;font-size:13px;outline:none;border-radius:12px 12px 0 0;">
  <div id="palette-results" style="max-height:320px;overflow-y:auto;"></div>
</div>
```

Add CSS for palette results in the `<style>` block:

```css
.palette-item { padding:8px 16px; cursor:pointer; font-size:13px; color:#ddd; border-left:3px solid transparent; }
.palette-item:hover, .palette-item.selected { background:#333; border-left-color:#7f6df2; }
.palette-item .palette-kind { font-size:11px; color:#666; margin-left:8px; }
.palette-item .palette-path { font-size:11px; color:#555; display:block; }
.palette-no-results { padding:16px; text-align:center; color:#666; font-size:13px; }
```

**5. Add fit-to-screen button.**

Add this BEFORE the `<div id="empty-state">` element:

```html
<button id="btn-fit" title="Fit to screen (F)" style="position:fixed;bottom:16px;left:16px;width:32px;height:32px;background:#333;border:1px solid #444;border-radius:8px;color:#999;cursor:pointer;z-index:100;display:flex;align-items:center;justify-content:center;">
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 3 21 3 21 9"/><polyline points="9 21 3 21 3 15"/><line x1="21" y1="3" x2="14" y2="10"/><line x1="3" y1="21" x2="10" y2="14"/></svg>
</button>
```

Add hover style for fit button in `<style>`:

```css
#btn-fit:hover { background: #444; color: #ddd; }
```
  </action>
  <verify>
    <automated>grep -c 'id="toggle-dead-code"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="toggle-blast-radius"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="expand-mode"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="command-palette"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="btn-fit"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="filter-fn"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="filter-edge-import"' /Users/vayu/Dev/Projects/cgraph/client/index.html && grep -c 'id="filter-dir"' /Users/vayu/Dev/Projects/cgraph/client/index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="toggle-dead-code"` checkbox in Analysis section
    - index.html contains `id="toggle-blast-radius"` checkbox in Analysis section
    - index.html contains `id="expand-mode"` select with options: Orbital, Force, Stacked
    - index.html contains `id="command-palette"` div with `id="palette-input"` and `id="palette-results"`
    - index.html contains `id="palette-backdrop"` div
    - index.html contains `id="btn-fit"` button
    - index.html contains `id="filter-fn"`, `id="filter-class"`, `id="filter-type"`, `id="filter-hook"`, `id="filter-enum"` checkboxes
    - index.html contains `id="filter-edge-import"`, `id="filter-edge-call"`, `id="filter-edge-typeref"` checkboxes
    - index.html contains `id="filter-dir"` text input
    - Analysis section appears AFTER Filters section and BEFORE Display section in DOM order
    - `.palette-item` CSS class defined
    - `.palette-no-results` CSS class defined
  </acceptance_criteria>
  <done>All Phase 5 HTML elements exist in index.html. Panel has Analysis section with dead code and blast radius toggles. Filters section has symbol/edge type checkboxes and directory input. Display section has expand mode dropdown. Command palette overlay and fit-to-screen button are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> DOM | Search input and directory filter are client-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | palette-input, header-search, filter-dir | mitigate | All user text displayed via D3 `.text()` or `textContent` (not `.html()` or `innerHTML`). Plans 03-05 enforce this in JS wiring. HTML elements are inert until JS connects them. |
</threat_model>

<verification>
- All element IDs exist in index.html (grep verification)
- HTML is valid (no unclosed tags)
- Panel section order: Filters, Analysis, Display, Forces
- Existing Phase 4 elements (#project-name, #stats, #tooltip, etc.) still present
</verification>

<success_criteria>
Opening index.html in a browser shows the restructured header with search input, navigation buttons, and quick-filter pills. The settings panel has Analysis and extended Filters sections. Command palette and fit-to-screen button are present (hidden until JS activates them).
</success_criteria>

<output>
After completion, create `.planning/phases/05-graph-interaction/05-02-SUMMARY.md`
</output>
