---
phase: 02-typescript-extractor
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - PARS-09
  - PARS-10

must_haves:
  truths:
    - "PARS-09 (multi-hop barrel resolution) is assigned to Phase 3 in both ROADMAP.md and REQUIREMENTS.md"
    - "PARS-10 (tsconfig path alias resolution) is assigned to Phase 3 in both ROADMAP.md and REQUIREMENTS.md"
    - "Phase 3 success criteria include barrel chain resolution and tsconfig alias resolution"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 requirements updated to include PARS-09, PARS-10"
      contains: "PARS-09"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated for PARS-09, PARS-10"
      contains: "Phase 2+3"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement IDs match across both files"
      pattern: "PARS-09.*Phase"
---

<objective>
Reassign PARS-09 and PARS-10 from Phase 2-only to Phase 2+3 split ownership in ROADMAP.md and REQUIREMENTS.md.

Purpose: Phase 2 intentionally emits raw single-hop re-export edges and raw alias paths (per D-25, D-28). The resolution work belongs in Phase 3's indexer layer. But the ROADMAP and REQUIREMENTS docs never assigned PARS-09/PARS-10 to Phase 3, leaving these requirements orphaned. This plan closes that documentation gap.

Output: Updated ROADMAP.md (Phase 3 requirements + success criteria) and REQUIREMENTS.md (traceability table).
</objective>

<execution_context>
@/Users/vayu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vayu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-typescript-extractor/02-CONTEXT.md
@.planning/phases/02-typescript-extractor/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 3 to include PARS-09 and PARS-10</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state of Phase 3 section)
    - .planning/phases/02-typescript-extractor/02-VERIFICATION.md (gap details)
    - .planning/phases/02-typescript-extractor/02-CONTEXT.md (D-25, D-28 decisions)
  </read_first>
  <action>
Edit the Phase 3 section of ROADMAP.md with the following changes:

1. **Requirements line**: Change from:
   `**Requirements**: ANLS-01, ANLS-02, ANLS-03, ANLS-04, ANLS-05, INFR-03`
   To:
   `**Requirements**: PARS-09, PARS-10, ANLS-01, ANLS-02, ANLS-03, ANLS-04, ANLS-05, INFR-03`

2. **Success Criteria**: Add two new criteria after the existing item 6. The existing 6 criteria remain unchanged. Add:
   `7. Re-export chains through barrel files (e.g., index.ts re-exporting from nested modules) are resolved to the true defining file — the graph contains no intermediate barrel-only edges (PARS-09)`
   `8. Import paths using tsconfig path aliases (e.g., @/components/Button) are resolved to the actual file path relative to the project root (PARS-10)`

3. **Phase 2 Success Criteria**: Leave SC5 and SC6 text unchanged. Phase 2 scope is correct per D-25/D-28 — the extractor emits raw edges, the indexer resolves them. The issue was the missing Phase 3 assignment, not the Phase 2 architecture.

Do NOT modify any other phase sections.
  </action>
  <verify>
    <automated>grep -c "PARS-09" .planning/ROADMAP.md | grep -q "^[1-9]" && grep -c "PARS-10" .planning/ROADMAP.md | grep -q "^[1-9]" && grep "Phase 3" .planning/ROADMAP.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Requirements.*PARS-09" .planning/ROADMAP.md returns a match in the Phase 3 section
    - grep "Requirements.*PARS-10" .planning/ROADMAP.md returns a match in the Phase 3 section
    - grep -c "PARS-09" .planning/ROADMAP.md returns at least 2 (Phase 2 + Phase 3)
    - The existing 6 Phase 3 success criteria are unchanged
    - Success criteria 7 contains "barrel" and "PARS-09"
    - Success criteria 8 contains "tsconfig" and "PARS-10"
  </acceptance_criteria>
  <done>Phase 3 Requirements line includes PARS-09 and PARS-10. Phase 3 has 8 success criteria covering barrel resolution and tsconfig alias resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table for PARS-09 and PARS-10</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current traceability table)
  </read_first>
  <action>
Edit the Traceability table in REQUIREMENTS.md:

1. Change the PARS-09 row from:
   `| PARS-09 | Phase 2 | Pending |`
   To:
   `| PARS-09 | Phase 2 (raw edges), Phase 3 (resolution) | Pending |`

2. Change the PARS-10 row from:
   `| PARS-10 | Phase 2 | Pending |`
   To:
   `| PARS-10 | Phase 2 (raw paths), Phase 3 (resolution) | Pending |`

This reflects the split ownership: Phase 2 extracts the raw data, Phase 3 resolves it. The requirement is satisfied when both phases complete.

Do NOT change any other rows in the table.
  </action>
  <verify>
    <automated>grep "PARS-09" .planning/REQUIREMENTS.md | grep "Phase 3" && grep "PARS-10" .planning/REQUIREMENTS.md | grep "Phase 3" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "PARS-09.*Phase 2.*Phase 3" .planning/REQUIREMENTS.md returns a match
    - grep "PARS-10.*Phase 2.*Phase 3" .planning/REQUIREMENTS.md returns a match
    - grep -c "Phase 2 (raw" .planning/REQUIREMENTS.md returns exactly 2
    - All other traceability rows are unchanged
  </acceptance_criteria>
  <done>PARS-09 and PARS-10 traceability rows show split ownership between Phase 2 and Phase 3. No requirements are orphaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies planning documentation only, no production code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | I (Information Disclosure) | .planning/ docs | accept | Planning docs are in-repo, no secrets, no PII |
</threat_model>

<verification>
- `grep "PARS-09" .planning/ROADMAP.md` shows Phase 3 section match
- `grep "PARS-10" .planning/ROADMAP.md` shows Phase 3 section match
- `grep "PARS-09.*Phase 2.*Phase 3" .planning/REQUIREMENTS.md` shows split ownership
- `grep "PARS-10.*Phase 2.*Phase 3" .planning/REQUIREMENTS.md` shows split ownership
- Phase 2 section of ROADMAP.md is unchanged
- Phase 3 now has 8 success criteria (was 6)
</verification>

<success_criteria>
PARS-09 and PARS-10 are no longer orphaned — both are explicitly assigned to Phase 3 for resolution, with Phase 2 owning raw extraction. The roadmap and requirements are consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-typescript-extractor/02-04-SUMMARY.md`
</output>
