---
phase: 04-http-server-browser-shell
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: []
autonomous: false
requirements:
  - VIZN-01
  - VIZN-02
  - VIZN-05
  - VIZN-06
  - VIZN-07
  - VIZN-08
  - INFR-02

must_haves:
  truths:
    - "Browser opens and renders a D3 force graph with no jitter"
    - "File nodes are visible as steel blue circles on dark background"
    - "Edges have visible arrowheads showing direction"
    - "Hover tooltip shows file path, exports, and edge counts"
    - "Legend panel is visible and collapsible"
    - "Directory halos toggle works"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Phase 4 deliverable -- confirm the browser graph renders correctly against a real codebase, validate all visual requirements, and verify the end-to-end flow from CLI scan to browser visualization.

Purpose: Many Phase 4 requirements (VIZN-01, VIZN-05, VIZN-06, VIZN-07) are visual and cannot be verified by automated tests alone. This checkpoint confirms the graph looks correct, the simulation is properly settled, arrowheads are visible, and the dark theme matches specifications.

Output: Visual confirmation that Phase 4 is complete, or a list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-http-server-browser-shell/04-UI-SPEC.md
@.planning/phases/04-http-server-browser-shell/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated integration checks</name>
  <files></files>
  <read_first>
    - crates/cli/src/main.rs (verify it compiles and has server startup)
    - crates/server/src/graph_api.rs (verify file_level_projection exists)
  </read_first>
  <action>
Run the following automated checks before asking the user for visual verification:

1. `cargo build -p cg` -- full binary compiles
2. `cargo test` -- all tests pass (server unit tests + existing CLI tests)
3. `cargo run -p cg -- --help` -- verify `--no-open` appears in help output
4. Start the server against one of the test fixture directories: `cargo run -p cg -- crates/ts-extractor/tests/fixtures --no-open &` then `curl -s http://localhost:3000/api/graph | python3 -m json.tool | head -30` to verify the JSON response shape has nodes, edges, stats, and project_name fields. Kill the background process after checking.
5. Verify the client assets are served: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` should return 200. `curl -s http://localhost:3000/ | head -5` should contain `<!DOCTYPE html>`.

Report results to the user before the visual checkpoint.
  </action>
  <verify>
    <automated>cd /Users/vayu/Dev/Projects/cgraph && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cg` exits 0
    - `cargo test` exits 0
    - `cargo run -p cg -- --help` output contains `--no-open`
    - `curl http://localhost:3000/api/graph` returns JSON with "nodes" and "edges" keys
    - `curl http://localhost:3000/` returns HTML containing "<!DOCTYPE html>"
  </acceptance_criteria>
  <done>All automated integration checks pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of D3 graph in browser</name>
  <files></files>
  <action>Present the visual verification checklist to the user after Task 1 automated checks pass.</action>
  <what-built>Complete Phase 4: HTTP server serving a D3 force-directed graph of the codebase. File nodes are color-coded circles on a dark canvas. Edges have arrowheads. Simulation is pre-settled. Hover tooltips show file details. Floating legend with directory halos toggle.</what-built>
  <how-to-verify>
    1. Run: `cargo run -p cg -- /path/to/a/typescript/project` (or use cgraph's own fixture directory: `cargo run -p cg -- crates/ts-extractor/tests/fixtures`)
    2. Browser should auto-open to http://localhost:3000
    3. Verify these visual requirements:

    **Canvas (D-50):**
    - [ ] Background is dark navy (#1a1a2e)
    - [ ] Text is white/light gray

    **Nodes (D-51, D-53, D-54):**
    - [ ] File nodes are steel blue circles
    - [ ] Larger nodes have more exports (size varies)
    - [ ] Labels below nodes show filenames

    **Edges (D-52, VIZN-05):**
    - [ ] Edges are subtle gray lines between nodes
    - [ ] Arrowheads are visible at the target end
    - [ ] Arrowheads are NOT hidden behind large nodes

    **Layout (VIZN-07, D-57):**
    - [ ] Graph appears instantly settled -- no jitter or animation on load
    - [ ] Nodes are well-spaced (no overlap)
    - [ ] Whole graph is visible

    **Header (D-58):**
    - [ ] Top bar shows project name (left) and stats (right)
    - [ ] Stats format: "{N} files . {N} symbols . {N} edges . {N}ms"

    **Tooltip (D-55):**
    - [ ] Hovering a node shows tooltip with full path, export summary, edge counts

    **Legend (D-59, D-56):**
    - [ ] Bottom-right legend panel is visible
    - [ ] Clicking the header collapses/expands it
    - [ ] "Directory halos" toggle exists
    - [ ] Toggling halos ON shows dashed boundary groups

    **Zoom (infrastructure):**
    - [ ] Scroll to zoom in/out works
    - [ ] Click-drag to pan works
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human visual verification"</automated>
  </verify>
  <done>User confirms all visual requirements pass or provides specific issues</done>
  <resume-signal>Type "approved" if everything looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Verification-only plan; no new code |
</threat_model>

<verification>
- All automated tests pass
- User confirms visual requirements match specifications
- End-to-end flow works: `cg <path>` -> server starts -> browser opens -> graph renders
</verification>

<success_criteria>
- User approves the visual verification checkpoint
- All 7 phase requirements verified: VIZN-01, VIZN-02, VIZN-05, VIZN-06, VIZN-07, VIZN-08, INFR-02
</success_criteria>

<output>
After completion, create `.planning/phases/04-http-server-browser-shell/04-04-SUMMARY.md`
</output>
